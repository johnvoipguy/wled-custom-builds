#!/bin/bash
# Deprecated legacy reference script.
# The original local-only quick_setup_wled-seeed-xiao.sh content was not present in repository history,
# so this placeholder exists only to preserve the legacy entry point name during the neutral restructure.

echo "quick_setup_wled-seeed-xiao.sh is deprecated and retained only as a legacy reference." >&2
echo "Use scripts/apply-target.sh and scripts/build-target.sh with targets/seeed-xiao-esp32s3 instead." >&2
exit 1
