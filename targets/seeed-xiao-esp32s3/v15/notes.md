# Seeed Xiao ESP32S3 v15

Starter notes for migrating earlier Xiao-specific work into the neutral multi-target layout.

- This directory replaces the old habit of keeping a long-lived Xiao branch or nested clone.
- Add target-specific partitions or usermods here only when they are ready to be tracked in `main`.
