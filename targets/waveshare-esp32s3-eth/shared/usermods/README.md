# Waveshare ESP32S3 Ethernet — Usermods

No Waveshare-specific usermod source is currently tracked in this repository.

If a Waveshare-only usermod is introduced later, add it under this directory with
its own `library.json`, then include it via `custom_usermods` in
`../platformio.env.ini`.
