# Waveshare ESP32S3 Ethernet — v15 assets

No preserved validated Waveshare v15 legacy firmware binaries are currently tracked.

If validated historical Waveshare v15 firmware files are recovered from legacy
locations (for example old `builds/` or `build_output/` exports), place them in:

`targets/waveshare-esp32s3-eth/v15/assets/legacy/`

and add a `legacy/README.md` describing provenance, validation status, and
flashing guidance.
